using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Poolable : MonoBehaviour
{   //별코드 없을거다. 얘는 그저 달려있으면 Pooling을 할 대상이라는 정도만 알게하면된다.
    public bool IsUsing; //아무것도 없으면 섭섭하니까, 그냥 쓰고있는지 정도만 담아두자.
}
